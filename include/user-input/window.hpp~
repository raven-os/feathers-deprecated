#pragma once

#include <waylan-egl.h>
#include <EGL/eegl.h>
#include <GL/gl.h>

class Window {
public:
  Window();
  ~Window();

  void draw();

private;
  static constexpr int WIDTH = 256;
  static constexpr int HEIGHT = 256;
  EGLContext egl_context;
  struct wl_surface *surface;
  struct wl_sheel_surface *shell_surface;
  struct wl_egl_window *egl_window;
  EGLSurface elg_surgace;

};
